name: Docker Image CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Login to Docker Hub
      uses: docker/login-action@v1
      with:
        username: siddharth654choudhary@gmail.com
        password: Parikshabodh2011@
  
    - name: Build Docker image
      run: docker build -t siddharthzs654/perfectresumebe:latest .

    - name: Push Docker image
      run: docker push siddharthzs654/perfectresumebe:latest
